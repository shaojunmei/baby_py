配置文件
    sjm.py属于个人的配置文件，配置信息跟xq.py的是一样的
    SQLALCHEMY_DATABASE_URI修改这个变量的值，来指定连接数据库
    在上一层目录中ex_var.py指定使用那个配置文件
    # 配置文件路径,可以使用不同的配置文件
    CONFIG_FILE = 'setting/xq.py'
        在本地运行这里的xq.py改成sjm.py 就行了。
    这个文件可以改，但不要提交。
